user  nginx;
worker_processes  1;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;

  log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

  access_log  /var/log/nginx/access.log  main;
  sendfile        on;
  keepalive_timeout  65;

  client_max_body_size 0;

  server {
    listen 80;
    server_name gitrepo.gbc.ca;
    return 301 https://$host$request_uri;
  }

  server {
    listen          443 ssl;
    server_name gitrepo.gbc.ca;

    ssl                   on;
    ssl_certificate       /etc/nginx/ssl/gitrepo.gbc.ca.crt;
    ssl_certificate_key   /etc/nginx/ssl/gitrepo.gbc.ca.key;
    ssl_session_timeout   5m;
    ssl_protocols       TLSv1.2;
    ssl_ciphers       HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers   on;
    location / {
        proxy_buffering off;
        proxy_redirect off;
        proxy_read_timeout 1500;
        proxy_send_timeout 1500;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_pass http://localhost:7990/;

        proxy_http_version 1.1;
    }
  }

  server {
    listen          443 ssl;
    server_name jira.gbc.ca;

    ssl                   on;
    ssl_certificate       /etc/nginx/ssl/gitrepo.gbc.ca.crt;
    ssl_certificate_key   /etc/nginx/ssl/gitrepo.gbc.ca.key;
    ssl_session_timeout   5m;
    ssl_protocols       TLSv1.2;
    ssl_ciphers       HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers   on;
    location / {
        proxy_buffering off;
        proxy_redirect off;
        proxy_read_timeout 1500;
        proxy_send_timeout 1500;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_pass http://localhost:8080/;

        proxy_http_version 1.1;
    }
  }

  server {
    listen          443 ssl;
    server_name confluence.gbc.ca;

    ssl                   on;
    ssl_certificate       /etc/nginx/ssl/gitrepo.gbc.ca.crt;
    ssl_certificate_key   /etc/nginx/ssl/gitrepo.gbc.ca.key;
    ssl_session_timeout   5m;
    ssl_protocols       TLSv1.2;
    ssl_ciphers       HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers   on;
    location / {
        proxy_buffering off;
        proxy_redirect off;
        proxy_read_timeout 1500;
        proxy_send_timeout 1500;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_pass http://localhost:8090/;

        proxy_http_version 1.1;
    }
  }

  server {
    listen          443 ssl;
    server_name jenkins1.gbc.ca;

    ssl                   on;
    ssl_certificate       /etc/nginx/ssl/gitrepo.gbc.ca.crt;
    ssl_certificate_key   /etc/nginx/ssl/gitrepo.gbc.ca.key;
    ssl_session_timeout   5m;
    ssl_protocols       TLSv1.2;
    ssl_ciphers       HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers   on;
    location / {
        proxy_buffering off;
        proxy_redirect off;
        proxy_read_timeout 1500;
        proxy_send_timeout 1500;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_pass http://localhost:8000/;

        proxy_http_version 1.1;
    }
  }

  server {
      listen          443 ssl;
      server_name artifactory.gbc.ca;

      ssl                   on;
      ssl_certificate       /etc/nginx/ssl/gitrepo.gbc.ca.crt;
      ssl_certificate_key   /etc/nginx/ssl/gitrepo.gbc.ca.key;
      ssl_session_timeout   5m;
      ssl_protocols       TLSv1.2;
      ssl_ciphers       HIGH:!aNULL:!MD5;
      ssl_prefer_server_ciphers   on;
      location / {
          proxy_buffering off;
          proxy_redirect off;
          proxy_read_timeout 1500;
          proxy_send_timeout 1500;
          proxy_set_header Host $host;
          proxy_set_header X-Forwarded-For $remote_addr;
          proxy_set_header X-Forwarded-Proto $scheme;
          proxy_set_header X-Original-URI $request_uri;
          proxy_set_header X-Real-IP $remote_addr;

          proxy_pass http://localhost:8081/;

          proxy_http_version 1.1;
      }
    }
    server {
    listen          443 ssl;
    server_name grafana.gbc.ca;

    ssl                   on;
    ssl_certificate       /etc/nginx/ssl/gitrepo.gbc.ca.crt;
    ssl_certificate_key   /etc/nginx/ssl/gitrepo.gbc.ca.key;
    ssl_session_timeout   5m;
    ssl_protocols       TLSv1.2;
    ssl_ciphers       HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers   on;
    location / {
        proxy_buffering off;
        proxy_redirect off;
        proxy_read_timeout 1500;
        proxy_send_timeout 1500;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Original-URI $request_uri;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_pass http://localhost:3000/;

        proxy_http_version 1.1;
    }
  }


}
